# Enter your code here. Read input from STDIN. Print output to STDOUT
from itertools import combinations_with_replacement

words, number= input().split()
word=sorted(words)
num=int(number)
for combo in combinations_with_replacement(word,num):
    print("".join(combo))
