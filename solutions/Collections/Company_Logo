import math
import os
import random
import re
import sys

import collections


if __name__ == '__main__':
    s = input()
    sl=list(s)
    c=collections.Counter(sl)

for key,value in sorted(c.items(),key=lambda x: (-x[1],x[0]))[:3]:
    print(key,value)
