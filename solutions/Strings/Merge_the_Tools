def merge_the_tools(string, k):
    import textwrap
    import math

    s_split=textwrap.wrap(string,k)
    for i in s_split:
        result=""
        for char in i:
            if char not in result:
                result=result+char

        print(result)
